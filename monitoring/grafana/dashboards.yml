# Grafana仪表板配置
apiVersion: 1

providers:
  - name: 'BYWG Dashboards'
    orgId: 1
    folder: 'BYWG'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
