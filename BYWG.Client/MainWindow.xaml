<Window x:Class="BYWG.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BYWG.Client"
        mc:Ignorable="d"
        Title="BYWG 工业协议网关" Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#FFF8F9FA">
    
    <Window.Resources>
        <!-- 商业风格样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFFF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF57C00"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFF44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFD32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF424242"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
        
        <Style x:Key="StatusLabel" TargetType="Label">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#FFF5F5F5"/>
            <Setter Property="VerticalGridLinesBrush" Value="#FFF5F5F5"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFF5F5F5"/>
            <Setter Property="Foreground" Value="#FF424242"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFF5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FFE3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="#FF2C3E50" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="BYWG" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="工业协议网关" FontSize="16" Foreground="#FFBDC3C7" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Label x:Name="ConnectionStatusLabel" Content="● 未连接" Foreground="#FFE74C3C" Style="{StaticResource StatusLabel}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 连接控制区域 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Label Content="服务地址:" Style="{StaticResource ModernLabel}"/>
                    <TextBox x:Name="ServerAddressTextBox" Text="http://localhost:5001" Width="200" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                
                <Button Grid.Column="1" Content="连接" Click="ConnectButton_Click" x:Name="ConnectButton" Style="{StaticResource ModernButton}"/>
                <Button Grid.Column="2" Content="断开" Click="DisconnectButton_Click" x:Name="DisconnectButton" IsEnabled="False" Style="{StaticResource DangerButton}"/>
                
                <!-- 功能按钮区域 -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="数据仪表板" Click="DataDashboardButton_Click" x:Name="DataDashboardButton" Style="{StaticResource WarningButton}"/>
                    <Button Content="节点管理" Click="OpenNodeManagerButton_Click" x:Name="OpenNodeManagerButton" Style="{StaticResource ModernButton}"/>
                </StackPanel>
                
                <!-- OPC UA服务器控制 -->
                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <Label Content="OPC UA:" Style="{StaticResource ModernLabel}"/>
                    <Button Content="启动" Click="StartOpcUaServerButton_Click" x:Name="StartOpcUaServerButton" Style="{StaticResource SuccessButton}"/>
                    <Button Content="停止" Click="StopOpcUaServerButton_Click" x:Name="StopOpcUaServerButton" IsEnabled="False" Style="{StaticResource DangerButton}"/>
                    <Label x:Name="ServerStatusLabel" Content="已停止" Foreground="#FFE74C3C" Style="{StaticResource StatusLabel}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：协议管理 -->
            <Border Grid.Column="0" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 协议管理标题 -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="📡" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="工业协议管理" FontSize="18" FontWeight="Bold" Foreground="#FF2C3E50" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="快速连接" Click="QuickConnectionButton_Click" x:Name="QuickConnectionButton" Style="{StaticResource SuccessButton}"/>
                            <Button Content="配置向导" Click="ProtocolWizardButton_Click" x:Name="ProtocolWizardButton" Style="{StaticResource ModernButton}"/>
                            <Button Content="刷新" Click="RefreshProtocolsButton_Click" x:Name="RefreshProtocolsButton" Style="{StaticResource ModernButton}"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 协议列表 -->
                    <DataGrid Grid.Row="1" x:Name="ProtocolDataGrid" AutoGenerateColumns="False" Margin="0,0,0,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="协议名称" Binding="{Binding Name}" Width="*" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding IsRunning}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Foreground" Value="#FF4CAF50"/>
                                                <Setter Property="Text" Value="运行中"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="False">
                                                <Setter Property="Foreground" Value="#FFE74C3C"/>
                                                <Setter Property="Text" Value="已停止"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- 协议操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="启动协议" Click="StartProtocolButton_Click" x:Name="StartProtocolButton" IsEnabled="False" Style="{StaticResource SuccessButton}"/>
                        <Button Content="停止协议" Click="StopProtocolButton_Click" x:Name="StopProtocolButton" IsEnabled="False" Style="{StaticResource DangerButton}"/>
                        <Button Content="删除协议" Click="DeleteProtocolButton_Click" x:Name="DeleteProtocolButton" IsEnabled="False" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 分隔线 -->
            <Border Grid.Column="1" Background="#FFE0E0E0" Width="1"/>

            <!-- 右侧：OPC UA节点管理 -->
            <Border Grid.Column="2" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 节点管理标题 -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="🔗" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="OPC UA节点管理" FontSize="18" FontWeight="Bold" Foreground="#FF2C3E50" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="添加节点" Click="AddNodeButton_Click" x:Name="AddNodeButton" Style="{StaticResource SuccessButton}"/>
                            <Button Content="刷新" Click="RefreshNodesButton_Click" x:Name="RefreshNodesButton" Style="{StaticResource ModernButton}"/>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 节点列表 -->
                    <DataGrid Grid.Row="1" x:Name="NodeDataGrid" AutoGenerateColumns="False" Margin="0,0,0,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="节点ID" Binding="{Binding NodeId.Identifier}" Width="2*" FontFamily="Consolas" FontSize="12"/>
                            <DataGridTextColumn Header="显示名称" Binding="{Binding DisplayName}" Width="1.5*" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="数据类型" Binding="{Binding DataType}" Width="120"/>
                            <DataGridTextColumn Header="当前值" Binding="{Binding CurrentValueString}" Width="1.5*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#FF2196F3"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="时间戳" Binding="{Binding CurrentValueTimestamp}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- 节点操作按钮 -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="读写节点" Click="ReadWriteNodeButton_Click" x:Name="ReadWriteNodeButton" IsEnabled="False" Style="{StaticResource ModernButton}"/>
                        <Button Content="删除节点" Click="DeleteNodeButton_Click" x:Name="DeleteNodeButton" IsEnabled="False" Style="{StaticResource DangerButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>